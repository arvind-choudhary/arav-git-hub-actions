on: push

jobs: 
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
            - name: Install
                run: npm install
            - name: Build
                run: npm run build
